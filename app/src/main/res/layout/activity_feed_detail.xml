<?xml version="1.0" encoding="utf-8"?>
<com.progress_view.ProgressRelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:progressActivity="http://schemas.android.com/apk/res-auto"
    android:id="@+id/progress"
    android:layout_width="match_parent"
    android:layout_height="match_parent">


    <ImageView
        android:id="@+id/image_view_feed_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:padding="@dimen/feed_details_padding"
        android:scaleType="center"
        progressActivity:srcCompat="@android:drawable/btn_plus" />

    <TextView
        android:id="@+id/text_view_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/image_view_feed_image"
        android:gravity="left"
        android:padding="@dimen/feed_details_padding" />

</com.progress_view.ProgressRelativeLayout>
